/*globals describe it */

const { expect } = require('chai')
const store = require('../public/store.js')

describe('reducers', () => {

  it('tests', () => {
    expect(store).to.not.equal(undefined)
  })

})
